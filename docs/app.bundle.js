/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./src/Battlefield.ts"
/*!****************************!*\
  !*** ./src/Battlefield.ts ***!
  \****************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nvar __importDefault = (this && this.__importDefault) || function (mod) {\n    return (mod && mod.__esModule) ? mod : { \"default\": mod };\n};\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\nconst Settlement_1 = __importDefault(__webpack_require__(/*! ./Settlement */ \"./src/Settlement.ts\"));\nclass Battlefield extends Settlement_1.default {\n    node_id = \"battlefield\";\n    static battlefields = [];\n    constructor(scene, name) {\n        super(scene, name);\n        this.setDimensions(new SRL_1.Vector2(20, 20));\n        Battlefield.battlefields.push(this);\n    }\n    //STATIC METHODS\n    /**\n     * Returns the list of battlefields\n     * @returns the complete list of battlefields\n     */\n    static getBattlefields() {\n        return this.battlefields;\n    }\n    Renderer = new SRL_1.SolidRenderer(\"black\");\n}\nexports[\"default\"] = Battlefield;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/Battlefield.ts?\n}");

/***/ },

/***/ "./src/Castle.ts"
/*!***********************!*\
  !*** ./src/Castle.ts ***!
  \***********************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nvar __importDefault = (this && this.__importDefault) || function (mod) {\n    return (mod && mod.__esModule) ? mod : { \"default\": mod };\n};\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\n/**\n * A castle is like a city, but has less defences and lower overall value.\n */\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\nconst Settlement_1 = __importDefault(__webpack_require__(/*! ./Settlement */ \"./src/Settlement.ts\"));\nclass Castle extends Settlement_1.default {\n    node_id = \"castle\";\n    static castles = [];\n    constructor(scene, name) {\n        super(scene, name);\n        this.setDimensions(new SRL_1.Vector2(12, 12));\n        Castle.castles.push(this);\n    }\n    //STATIC METHODS\n    /**\n     * Returns the list of castles\n     * @returns the complete list of castles\n     */\n    static getCastles() {\n        return this.castles;\n    }\n}\nexports[\"default\"] = Castle;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/Castle.ts?\n}");

/***/ },

/***/ "./src/City.ts"
/*!*********************!*\
  !*** ./src/City.ts ***!
  \*********************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nvar __importDefault = (this && this.__importDefault) || function (mod) {\n    return (mod && mod.__esModule) ? mod : { \"default\": mod };\n};\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\nconst Settlement_1 = __importDefault(__webpack_require__(/*! ./Settlement */ \"./src/Settlement.ts\"));\nclass City extends Settlement_1.default {\n    node_id = \"city\";\n    isCapital;\n    static cities = [];\n    constructor(scene, name, capital) {\n        super(scene, name);\n        this.isCapital = capital ?? false;\n        if (this.isCapital) {\n            this.setDimensions(new SRL_1.Vector2(25, 25));\n        }\n        else {\n            this.setDimensions(new SRL_1.Vector2(20, 20));\n        }\n        City.cities.push(this);\n    }\n    //STATIC METHODS\n    /**\n     * Returns the list of cities\n     * @returns the complete list of cities\n     */\n    static getCities() {\n        return this.cities;\n    }\n}\nexports[\"default\"] = City;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/City.ts?\n}");

/***/ },

/***/ "./src/GraphGenerator.ts"
/*!*******************************!*\
  !*** ./src/GraphGenerator.ts ***!
  \*******************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nvar __importDefault = (this && this.__importDefault) || function (mod) {\n    return (mod && mod.__esModule) ? mod : { \"default\": mod };\n};\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nconst Battlefield_1 = __importDefault(__webpack_require__(/*! ./Battlefield */ \"./src/Battlefield.ts\"));\nconst Castle_1 = __importDefault(__webpack_require__(/*! ./Castle */ \"./src/Castle.ts\"));\nconst City_1 = __importDefault(__webpack_require__(/*! ./City */ \"./src/City.ts\"));\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\nconst Settlement_1 = __importDefault(__webpack_require__(/*! ./Settlement */ \"./src/Settlement.ts\"));\nclass GraphGenerator {\n    kingdoms;\n    area;\n    constructor(kingdoms, pxWidth, pxHeight) {\n        this.kingdoms = kingdoms;\n        this.area = new SRL_1.Vector2(pxWidth, pxHeight);\n    }\n    Generate(seed, scene) {\n        //new instance of a seeded random number generator to ensure repeatable graphs\n        const random = new SRL_1.Utility.random.SeededRandomNG(String(seed));\n        console.log(`Beginning map generation with seed ${String(seed)}`);\n        //get starting locations for each kingdom\n        let start_locations = [];\n        for (let i = 0; i < this.kingdoms.length; i++) {\n            let recursions = 0;\n            while (true) {\n                //get new location candidate\n                random.clampBetween(0, this.area.x - 25);\n                let x = Math.floor(random.Next());\n                random.clampBetween(0, this.area.y - 25);\n                let y = Math.floor(random.Next());\n                let new_location = new SRL_1.Vector2(x, y);\n                //the first kingdom starting location is always valid\n                if (start_locations.length == 0) {\n                    start_locations.push(new_location);\n                    break;\n                }\n                //test if its too close to any other start locations\n                let valid = true;\n                for (let j = 0; j < start_locations.length; j++) {\n                    if (Math.abs(start_locations[j].x - new_location.x) < 200 && Math.abs(start_locations[j].y - new_location.y) < 200) {\n                        //too close to an existing start location\n                        valid = false;\n                        break;\n                    }\n                }\n                if (valid) {\n                    start_locations.push(new_location);\n                    break;\n                }\n                recursions++;\n                if (recursions > 100) {\n                    throw new Error(\"Unable to find a valid starting location after 100 attempts, there may be no valid starting locations left.\");\n                }\n            }\n        }\n        //valid start locations found\n        console.log(\"Found valid kingdom start locations\");\n        //set the location of each kingdoms capital to the start location\n        for (let i = 0; i < this.kingdoms.length; i++) {\n            this.kingdoms[i].capital.setLocation(start_locations[i]);\n        }\n        //each kingdom needs to place their sub cities in the voronoi cell of the kingdom with respect to the capital\n        for (let kingdom of this.kingdoms) {\n            for (let settlement of kingdom.getOwnedSettlements()) {\n                if (settlement instanceof City_1.default && !settlement.isCapital) {\n                    //is a subcity\n                    while (true) {\n                        //get a new random location on the map\n                        random.clampBetween(0, this.area.x);\n                        let test_x = Math.floor(random.Next());\n                        random.clampBetween(0, this.area.y);\n                        let test_y = Math.floor(random.Next());\n                        let test_location = new SRL_1.Vector2(test_x, test_y);\n                        //set the settlements location to the random location\n                        settlement.setLocation(test_location);\n                        //check that the placement is valid\n                        if (this.checkValidPlacement(settlement)) {\n                            break;\n                        }\n                    }\n                }\n            }\n        }\n        console.log(\"Populated map with cities\");\n        //each kingdom needs a set of castles\n        for (let kingdom of this.kingdoms) {\n            for (let settlement of kingdom.getOwnedSettlements()) {\n                if (settlement instanceof Castle_1.default) {\n                    //is a castle\n                    while (true) {\n                        //get a new random location on the map\n                        random.clampBetween(0, this.area.x);\n                        let test_x = Math.floor(random.Next());\n                        random.clampBetween(0, this.area.y);\n                        let test_y = Math.floor(random.Next());\n                        let test_location = new SRL_1.Vector2(test_x, test_y);\n                        //set the settlements location to the random location\n                        settlement.setLocation(test_location);\n                        //check that the placement is valid\n                        if (this.checkValidPlacement(settlement)) {\n                            break;\n                        }\n                    }\n                }\n            }\n        }\n        console.log(\"Populated map with castles\");\n        //connections\n        //for each kingdom\n        for (let kingdom of this.kingdoms) {\n            //connect each city owned by the kingdom\n            for (let i of kingdom.getCities()) {\n                for (let j of kingdom.getCities()) {\n                    if (i !== j && !i.getConnection(j)) {\n                        i.addConnection(j);\n                    }\n                }\n            }\n            //connect castles to their closest city\n            for (let i of kingdom.getCastles()) {\n                let closest_city;\n                let closest_dist = Infinity;\n                for (let j of kingdom.getCities()) {\n                    if (j.distanceTo(i) < closest_dist) {\n                        closest_dist = j.distanceTo(i);\n                        closest_city = j;\n                    }\n                }\n                i.addConnection(closest_city);\n            }\n        }\n        console.log(\"Connected cities and castles\");\n        //connect each settlement to the closest enemy settlement\n        for (let s of Settlement_1.default.getSettlements()) {\n            let closest;\n            let closest_dist = Infinity;\n            for (let s2 of Settlement_1.default.getSettlements()) {\n                if (s.getKingdom() !== s2.getKingdom() && s.distanceTo(s2) < closest_dist) {\n                    closest = s2;\n                    closest_dist = s.distanceTo(s2);\n                }\n            }\n            if (!s.getConnection(closest)) {\n                s.addConnection(closest);\n            }\n        }\n        console.log(\"Created some bridges between kingdoms\");\n        //if multiple external connections to a settlement, only keep the closest one\n        for (let s of Settlement_1.default.getSettlements()) {\n            let settlement_conections = s.getConnections();\n            let external_connections = [];\n            for (let e_c of settlement_conections) {\n                if (e_c.settlement1.getKingdom() != e_c.settlement2.getKingdom()) {\n                    //is external connection\n                    external_connections.push(e_c);\n                }\n            }\n            let shortest;\n            let shortest_weight = Infinity;\n            for (let c of external_connections) {\n                if (c.weight < shortest_weight) {\n                    shortest = c;\n                    shortest_weight = c.weight;\n                }\n            }\n            for (let c of external_connections) {\n                if (c !== shortest) {\n                    s.removeConnection(c.settlement2);\n                }\n            }\n        }\n        console.log(\"Softened choke-points\");\n        //create battlefields\n        /**\n         * Connections of longer distance than MAX_DIST are severed and instead now both link to a battlefield.\n         * Battlefields are linked to the two nodes that formed the original connection, as well as the next closest node.\n         */\n        const MAX_DIST = 200;\n        while (true) {\n            let bfs_created = 0;\n            let settlements = Settlement_1.default.getSettlements();\n            for (let settlement of settlements) {\n                let settlement_cons = settlement.getConnections();\n                for (let connection of settlement_cons) {\n                    if (connection.weight > MAX_DIST) {\n                        //need to place a battlefield\n                        //sever the connection\n                        connection.settlement1.removeConnection(connection.settlement2);\n                        //create new battlefield between them\n                        let bf = new Battlefield_1.default(scene, \"battlefield\");\n                        let x = Math.floor((connection.settlement1.getCentrePoint().x + connection.settlement2.getCentrePoint().x) / 2);\n                        let y = Math.floor((connection.settlement1.getCentrePoint().y + connection.settlement2.getCentrePoint().y) / 2);\n                        bf.setLocation(new SRL_1.Vector2(x, y));\n                        //connect the settlements to the battlefield\n                        connection.settlement1.addConnection(bf);\n                        connection.settlement2.addConnection(bf);\n                        //add a third connection to the next nearest node\n                        let nearest_node;\n                        let nearest_dist = Infinity;\n                        for (let test_settlement of Settlement_1.default.getSettlements()) {\n                            if (test_settlement !== connection.settlement1 && test_settlement !== connection.settlement2 && test_settlement !== bf) {\n                                if (bf.distanceTo(test_settlement) < nearest_dist) {\n                                    nearest_node = test_settlement;\n                                    nearest_dist = bf.distanceTo(test_settlement);\n                                }\n                            }\n                        }\n                        bf.addConnection(nearest_node);\n                        bfs_created++;\n                    }\n                }\n            }\n            if (bfs_created == 0) {\n                //no new battlefields created on this itteration, all have been created\n                break;\n            }\n        }\n        console.log(\"Created battlefields\");\n        //finally, check that the graph is complete. If so, return the graph, if not, return null.\n        let graph_connected = this.ensureGraphConnected();\n        if (graph_connected) {\n            console.log(\"Generated graph is valid!\");\n        }\n        else {\n            console.log(\"Generated graph contains some isolated nodes. Might still be interesting!\");\n        }\n        return graph_connected;\n    }\n    /**\n     * Returns the kingdom who's capital is closest to the given point\n     * @param point a point on the map plane\n     * @returns the kingdom whose capital is closest to the given point\n     */\n    getKingdomOfPoint(point) {\n        let closest_kingdom;\n        let closest_distance = Infinity;\n        //for each kingdom\n        for (let i = 0; i < this.kingdoms.length; i++) {\n            //compute the distance as the crow flies from the point to the kingdoms capital\n            let trial_kingdom = this.kingdoms[i];\n            let dx = trial_kingdom.capital.getCentrePoint().x - point.x;\n            let dy = trial_kingdom.capital.getCentrePoint().y - point.y;\n            let mag = dx * dx + dy * dy;\n            //if the distance is closer than the previous closest, update\n            if (mag < closest_distance) {\n                closest_kingdom = trial_kingdom;\n                closest_distance = mag;\n            }\n        }\n        //return the kingdom closest to the point\n        return closest_kingdom;\n    }\n    /**\n     * Checks the settlements current location is valid given a set of rules.\n     * @param settlement a settlement\n     * @returns true if the settlements current location is valid, else false\n     */\n    checkValidPlacement(settlement) {\n        //check if location is within voronoi cell of the kingdom, if not its invalid\n        if (this.getKingdomOfPoint(settlement.getLocation()) !== settlement.getKingdom()) {\n            return false;\n        }\n        //check that the placement is within the bounds of the map\n        if (settlement.getLocation().x < 0 || settlement.getLocation().x + 20 > this.area.x || settlement.getLocation().y < 0 || settlement.getLocation().y + 20 > this.area.y) {\n            return false;\n        }\n        //finally, check if its new location causes it to collide with another settlement, if so, its invalid\n        for (let s of Settlement_1.default.getSettlements()) {\n            if (s !== settlement && settlement.collision(s).length > 0) {\n                return false;\n            }\n        }\n        //location passes all tests\n        return true;\n    }\n    ensureGraphConnected() {\n        let settlements = [...Settlement_1.default.getSettlements()];\n        //random starting place for graph traversal\n        let root = SRL_1.Utility.random.randItem(settlements);\n        //set it exploring the network\n        explore(root);\n        if (settlements.length > 0) {\n            //some nodes were left unvisited, the graph is not connected.\n            return false;\n        }\n        return true;\n        function explore(node) {\n            //remove this from the list of settlements, as its been visited\n            SRL_1.Utility.array.removeItem(settlements, node);\n            for (let connection of node.getConnections()) {\n                if (settlements.includes(connection.settlement2)) {\n                    explore(connection.settlement2);\n                }\n            }\n        }\n    }\n}\nexports[\"default\"] = GraphGenerator;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/GraphGenerator.ts?\n}");

/***/ },

/***/ "./src/Kingdom.ts"
/*!************************!*\
  !*** ./src/Kingdom.ts ***!
  \************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\n/**\n * Kingdom class holds data on the distinct kingdoms. Each settlement or lord is linked to a kingdom.\n */\nvar __importDefault = (this && this.__importDefault) || function (mod) {\n    return (mod && mod.__esModule) ? mod : { \"default\": mod };\n};\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nconst Castle_1 = __importDefault(__webpack_require__(/*! ./Castle */ \"./src/Castle.ts\"));\nconst City_1 = __importDefault(__webpack_require__(/*! ./City */ \"./src/City.ts\"));\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\nclass Kingdom {\n    name;\n    capital;\n    colour;\n    settlements = [];\n    lords = [];\n    /**\n     * @param name the kingdoms name\n     * @param colour the colour of the kingdom\n     */\n    constructor(name, colour) {\n        this.name = name;\n        this.colour = colour;\n    }\n    /**\n     * Adds a settlement to the kingdom\n     * @param settlement the settlement to add to the kingdom\n     * @returns True of the operation was successful, else false\n     */\n    addSettlement(settlement) {\n        if (this.settlements.includes(settlement)) {\n            console.error(\"Error, tried to add a settlement to a kingdom that already owns it.\", settlement, this);\n            return false;\n        }\n        this.settlements.push(settlement);\n        settlement.setKingdom(this);\n        if (settlement instanceof City_1.default && settlement.isCapital && this.capital == null) {\n            this.capital = settlement;\n        }\n        return true;\n    }\n    /**\n     * Removes the settlement from the kingdom\n     * @param settlement the settlement to unlink from the kingdom\n     * @returns true if the operation was successful, else false\n     */\n    removeSettlement(settlement) {\n        if (!this.settlements.includes(settlement)) {\n            console.error(\"Error, tried to remove a settlement from a kingdom that doesnt own it.\", settlement, this);\n            return false;\n        }\n        SRL_1.Utility.array.removeItem(this.settlements, settlement);\n        return true;\n    }\n    /**\n     * Adds a lord to the kingdom\n     * @param lord the lord to add to the kingdom\n     * @returns true if the operation was successful, else false\n     */\n    addLord(lord) {\n        if (this.lords.includes(lord)) {\n            console.error(\"Error, tried to add a lord to a kingdom of which it already is a part of.\", lord, this);\n            return false;\n        }\n        this.lords.push(lord);\n        return true;\n    }\n    /**\n     * Gets the list of settlements that the kingdom owns\n     * @returns the settlements currently associated with the kingdom\n     */\n    getOwnedSettlements() {\n        return this.settlements;\n    }\n    /**\n     * Gets the list of cities currently owned by this kingdom\n     * @returns the list of cities\n     */\n    getCities() {\n        let out = [];\n        for (let s of this.settlements) {\n            if (s instanceof City_1.default) {\n                out.push(s);\n            }\n        }\n        return out;\n    }\n    /**\n     * Gets the list of castles currently owned by this kingdom\n     * @returns the list of castles\n     */\n    getCastles() {\n        let out = [];\n        for (let s of this.settlements) {\n            if (s instanceof Castle_1.default) {\n                out.push(s);\n            }\n        }\n        return out;\n    }\n}\nexports[\"default\"] = Kingdom;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/Kingdom.ts?\n}");

/***/ },

/***/ "./src/Lord.ts"
/*!*********************!*\
  !*** ./src/Lord.ts ***!
  \*********************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nvar __importDefault = (this && this.__importDefault) || function (mod) {\n    return (mod && mod.__esModule) ? mod : { \"default\": mod };\n};\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nconst Battlefield_1 = __importDefault(__webpack_require__(/*! ./Battlefield */ \"./src/Battlefield.ts\"));\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\n/**\n * A lord is an autonomous unit that partakes in campaigns and holds a warband\n */\nclass Lord {\n    static lords = [];\n    name;\n    kingdom;\n    is_king;\n    warband_size = 0;\n    location; //the settlement the lord is currently at\n    in_field = false; //whether the lord is currently outside (in_field) or inside (!in_field) the settlement.\n    home; //the settlement considered the lord's \"home\"\n    constructor(name, starting_kingdom, isKing = false) {\n        this.name = name;\n        this.is_king = isKing;\n        this.setKingdom(starting_kingdom);\n        Lord.lords.push(this);\n    }\n    /**\n     * Called when its time for the lord to act. Determines the lord's best action to take this \"turn\" and then acts upon it.\n     */\n    Act() {\n        //dummy logic picks a random adjacent settlement and moves to it\n        if (!this.in_field) {\n            this.exitSettlement();\n        }\n        else {\n            let destination = SRL_1.Utility.random.randItem(this.location.getConnections()).settlement2;\n            this.moveTo(destination);\n        }\n    }\n    /**\n     * Assigns the lord to a new kingdom\n     * @param kingdom the kingdom the lord should now be assigned to\n     * @returns true if the operation was successful, else false\n     */\n    setKingdom(kingdom) {\n        this.kingdom = kingdom;\n        return this.kingdom.addLord(this);\n    }\n    getKingdom() {\n        return this.kingdom;\n    }\n    /**\n     * Lord enters the settlement its currently at. Only possible if the settlement is of type castle/city and is of the same kingdom\n     * @returns true if the operation was successful, else false\n     */\n    enterSettlement() {\n        if (this.location.getKingdom() != this.kingdom) {\n            //lord is not in the same kingdom as the settlement, illegal operation\n            return false;\n        }\n        else if (this.location instanceof Battlefield_1.default) {\n            //cannot enter battlefields\n            return false;\n        }\n        if (!this.location.field_lords.includes(this)) {\n            //lord is not outside this settlement, how can it enter?\n            console.error(\"Error, lord is trying to enter a settlement when they aren't already outside it\", this.location, this);\n            return false;\n        }\n        this.location.garrison_lords.push(this);\n        SRL_1.Utility.array.removeItem(this.location.field_lords, this);\n        this.in_field = false;\n        return true;\n    }\n    /**\n     * Lord exits its current settlement to return to the field. Only possible if the settlement is not under siege.\n     * @returns true if the operation was successful, else false\n     */\n    exitSettlement() {\n        if (this.location.besieged) {\n            //cannot exit this settlement as it's besieged\n            return false;\n        }\n        else if (!this.location.garrison_lords.includes(this)) {\n            //lord is not inside this settlement, how can it leave?\n            console.error(\"Error, lord is trying to leave a settlement that they arent in!\", this.location, this);\n            return false;\n        }\n        this.location.field_lords.push(this);\n        SRL_1.Utility.array.removeItem(this.location.garrison_lords, this);\n        this.in_field = true;\n        return true;\n    }\n    /**\n     * Moves the lord to a new settlement\n     * @param settlement the new settlement the lord is being moved to\n     * @returns true if the operation was successful, else false\n     */\n    moveTo(settlement) {\n        if (this.location === settlement) {\n            console.error(\"Unable to move the lord as they are currently at the settlement they wish to move to!\", this.location, this);\n            return false;\n        }\n        else if (this.location) {\n            if (this.location.garrison_lords.includes(this)) {\n                console.error(\"Unable to move the lord, as they are currently inside a settlement!\", this.location, this);\n                return false;\n            }\n            //remove the lord from their current location first\n            SRL_1.Utility.array.removeItem(this.location.field_lords, this);\n        }\n        //then add them to the new settlement\n        this.location = settlement;\n        settlement.field_lords.push(this);\n        return true;\n    }\n    /**\n     * Gets the list of all lords in the game\n     * @returns the list of all lords in the game\n     */\n    static getLords() {\n        return this.lords;\n    }\n}\nexports[\"default\"] = Lord;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/Lord.ts?\n}");

/***/ },

/***/ "./src/Settlement.ts"
/*!***************************!*\
  !*** ./src/Settlement.ts ***!
  \***************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\n/**\n * Settlement class defines default behaviour for different settlement types\n */\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\nclass Settlement extends SRL_1.Component {\n    kingdom = null;\n    name;\n    scene;\n    connections = [];\n    nametag = null;\n    garrison = 0;\n    garrison_lords = []; //garrison lords are INSIDE the settlement, defending/resting. Must be lords of the same kingdom as the settlement.\n    field_lords = []; //field lords are OUTSIDE the settlement, can be lords of other kingdoms.\n    besieged = false;\n    static settlements = [];\n    constructor(scene, name) {\n        super(scene);\n        this.name = name;\n        this.scene = scene;\n        //set its initial location very far away, helps with graph generation\n        this.setLocation(new SRL_1.Vector2(-100000, -100000));\n        Settlement.settlements.push(this);\n    }\n    /**\n     * Gets the kingdom that owns this settlement\n     * @returns the settlement's current kingdom\n     */\n    getKingdom() {\n        return this.kingdom;\n    }\n    /**\n     * Assigns the settlement to the new kingdom\n     * @param kingdom the new kingdom owner of the settlement\n     */\n    setKingdom(kingdom) {\n        this.Renderer = new SRL_1.SolidRenderer(kingdom.colour);\n        this.kingdom = kingdom;\n        return true;\n    }\n    /**\n     * Gets the Connection object if there is a connection between this Settlement and the given Settlement, else null\n     * @param settlement the settlement to check if there is a connection with\n     * @returns the Connection if one exists, else false\n     */\n    getConnection(settlement) {\n        for (let connection of this.connections) {\n            if (connection.settlement2 == settlement) {\n                return connection;\n            }\n        }\n        return null;\n    }\n    /**\n     * Gets all connections for the node\n     * @returns the connections of the node\n     */\n    getConnections() {\n        return this.connections;\n    }\n    /**\n     * Forms a connection between this settlement and another settlement\n     * @param settlement the settlement to form a connection with\n     * @returns true if the operation was successful, else false\n     */\n    addConnection(settlement) {\n        if (this.getConnection(settlement)) {\n            console.error(\"Error, tried to create a connection between two settlements where a connection already exists\", this, settlement);\n            return false;\n        }\n        if (this == settlement) {\n            console.error(\"Error, settlement tried to create a connection with itself\", this);\n            return false;\n        }\n        const dist = this.distanceTo(settlement);\n        this.connections.push({ settlement1: this, settlement2: settlement, weight: dist });\n        settlement.connections.push({ settlement1: settlement, settlement2: this, weight: dist });\n        return true;\n    }\n    /**\n     * Removes a connection between this settlement, and the given settlement\n     * @param settlement the settlement to remove the connection with\n     * @returns true if the operation was successful, else false\n     */\n    removeConnection(settlement) {\n        let connection = this.getConnection(settlement);\n        let remote_connection = settlement.getConnection(this);\n        if (!connection || !remote_connection) {\n            console.error(\"Error, unable to remove a connection between settlements when a connection does not exist\", this, settlement);\n            return false;\n        }\n        SRL_1.Utility.array.removeItem(this.connections, connection);\n        SRL_1.Utility.array.removeItem(settlement.connections, remote_connection);\n        return true;\n    }\n    /**\n     * Measures the distance between this settlement and another given settlement\n     * @param settlement the settlement to measure the distance to\n     * @returns the distance between the two settlements\n     */\n    distanceTo(settlement) {\n        let dx = this.getCentrePoint().x - settlement.getCentrePoint().x;\n        let dy = this.getCentrePoint().y - settlement.getCentrePoint().y;\n        return Math.sqrt(dx * dx + dy * dy);\n    }\n    //===STATIC METHODS===\n    /**\n     * Gets the list of all created settlements\n     * @returns a list of settlements\n     */\n    static getSettlements() {\n        return this.settlements;\n    }\n    MouseUp(event) {\n        console.log(this);\n    }\n    Update(info) {\n        if (this.isPointInComponent(info.engine.getMousePoint(), info.engine.getCameraPos()) && this.nametag == null) {\n            //create nametag\n            this.nametag = new SRL_1.TextComponent(this.scene);\n            this.nametag.setText(this.name);\n            this.nametag.setLocation(new SRL_1.Vector2(this.getLocation().x, this.getLocation().y + 25));\n        }\n        else if (this.nametag !== null && !this.isPointInComponent(info.engine.getMousePoint(), info.engine.getCameraPos())) {\n            //remove nametag\n            this.nametag.disable();\n            this.nametag = null;\n        }\n    }\n    LateUpdate(info) {\n        const ctx = info.engine.getRenderingContext();\n        //draw the settlements connections\n        for (let c of this.connections) {\n            let colour = \"black\";\n            if (c.settlement1.node_id != \"battlefield\" && c.settlement2.node_id !== \"battlefield\" && (c.settlement1.getKingdom() == c.settlement2.getKingdom())) {\n                colour = c.settlement1.getKingdom().colour;\n            }\n            //draw\n            ctx.beginPath();\n            ctx.moveTo(c.settlement1.getCentrePoint().x, c.settlement1.getCentrePoint().y);\n            ctx.lineTo(c.settlement2.getCentrePoint().x, c.settlement2.getCentrePoint().y);\n            ctx.lineWidth = 1;\n            ctx.strokeStyle = colour;\n            ctx.stroke();\n        }\n    }\n}\nexports[\"default\"] = Settlement;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/Settlement.ts?\n}");

/***/ },

/***/ "./src/index.ts"
/*!**********************!*\
  !*** ./src/index.ts ***!
  \**********************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\n/**\n * Entrypoint for app\n */\nvar __importDefault = (this && this.__importDefault) || function (mod) {\n    return (mod && mod.__esModule) ? mod : { \"default\": mod };\n};\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nconst Castle_1 = __importDefault(__webpack_require__(/*! ./Castle */ \"./src/Castle.ts\"));\nconst City_1 = __importDefault(__webpack_require__(/*! ./City */ \"./src/City.ts\"));\nconst GraphGenerator_1 = __importDefault(__webpack_require__(/*! ./GraphGenerator */ \"./src/GraphGenerator.ts\"));\nconst Kingdom_1 = __importDefault(__webpack_require__(/*! ./Kingdom */ \"./src/Kingdom.ts\"));\nconst SRL_1 = __webpack_require__(/*! ./lib/SRL */ \"./src/lib/SRL/index.ts\");\nconst Lord_1 = __importDefault(__webpack_require__(/*! ./Lord */ \"./src/Lord.ts\"));\nlet KINGDOM_NAMES = [\"Nords\", \"Swadia\", \"Rhodoks\", \"Kergit\", \"Vaegirs\", \"Sarranid\", \"Aserai\", \"Battania\", \"Khuzait\", \"Sturgia\", \"Vlandia\"];\nlet SETTLEMENT_NAMES = [\n    \"Alderford\", \"Brackenwall\", \"Crestholm\", \"Dunmere\", \"Eldenwatch\", \"Fallowridge\", \"Glenmarch\", \"Hartwell\", \"Ironstead\", \"Kingsbarrow\",\n    \"Larkhollow\", \"Marrowfield\", \"Oakenshire\", \"Redwyn\", \"Stonebrook\", \"Thornwick\", \"Westerholt\", \"Windmere\", \"Yarrowbridge\", \"Highwarden\",\n    \"Bjornvik\", \"Drakkensund\", \"Frosthalla\", \"Gunnarstad\", \"Hjalmfjord\", \"Isenfell\", \"Jorvikholm\", \"Kaldsund\", \"Norrhalla\", \"Skjoldheim\",\n    \"Stormvik\", \"Thrymfell\", \"Ulfsfjord\", \"Vargstad\", \"Wintermark\", \"Yngarstead\", \"Rimehaven\", \"Hrafnborg\", \"Skarholm\", \"Fjellgard\",\n    \"Arenograd\", \"Belgorin\", \"Chernovar\", \"Darskova\", \"Elyagrad\", \"Frostov\", \"Gorinsk\", \"Karshevik\", \"Lodovik\", \"Morozhyn\",\n    \"Novarusk\", \"Ostvelka\", \"Pereskyn\", \"Ravnygrad\", \"Svetlovo\", \"Tarnovik\", \"Velgorod\", \"Veshtova\", \"Zimnyholm\", \"Krestov\",\n    \"Barrowcliff\", \"Cragspire\", \"Dellharth\", \"Fennrock\", \"Greenhollow\", \"Highspire\", \"Keldbarrow\", \"Mossreach\", \"Pinewatch\", \"Ridgefall\",\n    \"Rockhaven\", \"Shardpeak\", \"Southmere\", \"Stonehollow\", \"Tarnspire\", \"Undercrest\", \"Valemont\", \"Wyrmcliff\", \"Hillwarden\", \"Briarhold\",\n    \"Aksun\", \"Batu Khar\", \"Chagan Ordu\", \"Dalgatai\", \"Erden Tal\", \"Gurkhun\", \"Kharzun\", \"Muratai\", \"Narak Pass\", \"Ordoshar\",\n    \"Qazhen\", \"Sarnak\", \"Tolgatai\", \"Uldan Steppe\", \"Varkhun\", \"Yurtai\", \"Zalghar\", \"Keshun Ordu\", \"Tamarak\", \"Ulzar\"\n];\nlet LORD_NAMES = [\n    \"Alaric\", \"Berengar\", \"Cedric\", \"Dalmor\", \"Edric\", \"Fendrel\", \"Garran\", \"Halden\", \"Isenbard\", \"Jorlan\",\n    \"Kaelric\", \"Lothar\", \"Marnic\", \"Norvald\", \"Osric\", \"Perrin\", \"Quinvar\", \"Roderin\", \"Selwick\", \"Tarmund\",\n    \"Bjornulf\", \"Dagfinn\", \"Eirikson\", \"Frode\", \"Gunnvald\", \"Hrolf\", \"Ingmar\", \"Jorund\", \"Ketil\", \"Leifvar\",\n    \"Mjorn\", \"Orvar\", \"Ragnvald\", \"Sigmar\", \"Torsten\", \"Ulfrik\", \"Vargir\", \"Yngrim\", \"Skarde\", \"Thrain\",\n    \"Antonin\", \"Borislav\", \"Chernov\", \"Davorin\", \"Eldarov\", \"Fyodorin\", \"Gavril\", \"Ilyanov\", \"Kazimir\", \"Leonid\",\n    \"Mirovan\", \"Olegar\", \"Petrovic\", \"Radovan\", \"Sergein\", \"Tovar\", \"Vasilin\", \"Yaroslav\", \"Zorin\", \"Kresimir\",\n    \"Ardan\", \"Belric\", \"Corvan\", \"Durnwald\", \"Elthor\", \"Fenric\", \"Galdemar\", \"Harvold\", \"Jastin\", \"Keldran\",\n    \"Lorwick\", \"Marvold\", \"Nerrin\", \"Orwyn\", \"Pellard\", \"Rannic\", \"Stenwald\", \"Torwick\", \"Ulmar\", \"Werrin\",\n    \"Aksai\", \"Batujin\", \"Chagan\", \"Dalgor\", \"Erketai\", \"Gansukh\", \"Harkun\", \"Jirgal\", \"Kharun\", \"Murtai\",\n    \"Nergui\", \"Orlok\", \"Qadan\", \"Sargul\", \"Torgai\", \"Uldar\", \"Varkhun\", \"Yusai\", \"Zolgar\", \"Temur\"\n];\nclass Main {\n    static Main() {\n        console.log(\"Hello, World!\");\n        //=============SETTINGS=============\n        //define number of kingdoms\n        const no_kingdoms = 6;\n        //number of sub-cities (non capitals, still cities) each kingdom could have\n        const no_subcities = [2]; //[2, 3];\n        //number of castles each kingdom could have\n        const no_castles = [3]; //[3, 4];\n        //width of the display\n        const WINDOW_W = 1200;\n        //height of the display\n        const WINDOW_H = 800;\n        //graph generator seed\n        const GRAPH_SEED = Math.random();\n        //==================================\n        let engine = new SRL_1.Engine(document.querySelector(\"main\"));\n        engine.Init({\n            height: WINDOW_H,\n            width: WINDOW_W,\n            FPSCap: 60,\n            background: \"#eaeaea\"\n        });\n        const scene = new SRL_1.Scene(engine);\n        //create kingdoms\n        let kingdoms = [];\n        for (let i = 0; i < no_kingdoms; i++) {\n            let name;\n            let colour;\n            //get random kingdom name\n            name = SRL_1.Utility.random.randItem(KINGDOM_NAMES);\n            SRL_1.Utility.array.removeItem(KINGDOM_NAMES, name);\n            //get random kingdom colour\n            let r = SRL_1.Utility.random.randInt(0, 255);\n            let g = SRL_1.Utility.random.randInt(0, 255);\n            let b = SRL_1.Utility.random.randInt(0, 255);\n            colour = `rgb(${r}, ${g}, ${b})`;\n            const kingdom = new Kingdom_1.default(name, colour);\n            kingdoms.push(kingdom);\n            console.log(`Created new kingdom \"${name}\"`, kingdom);\n        }\n        //create settlements\n        for (let kingdom of kingdoms) {\n            //each kingdom needs a capital\n            let name = this.getNewSettlementName();\n            let capital = new City_1.default(scene, name, true);\n            kingdom.addSettlement(capital);\n            //each kingdom needs some sub cities\n            for (let i = 0; i < SRL_1.Utility.random.randItem(no_subcities); i++) {\n                let name = this.getNewSettlementName();\n                let city = new City_1.default(scene, name);\n                kingdom.addSettlement(city);\n            }\n            //each kingdom needs some castles\n            for (let i = 0; i < SRL_1.Utility.random.randItem(no_castles); i++) {\n                let name = this.getNewSettlementName();\n                let castle = new Castle_1.default(scene, name);\n                kingdom.addSettlement(castle);\n            }\n        }\n        //create lords\n        const LORDS_PER_KINGDOM = 10;\n        for (let k of kingdoms) {\n            let settlements = k.getOwnedSettlements();\n            for (let i = 0; i < LORDS_PER_KINGDOM; i++) {\n                let lord;\n                if (i == 0) {\n                    //create king\n                    let name = this.getNewLordName();\n                    lord = new Lord_1.default(name, k, true);\n                }\n                else {\n                    //create lord\n                    let name = this.getNewLordName();\n                    lord = new Lord_1.default(name, k, false);\n                }\n                //set the lords \"home\" and move them there\n                if (i < settlements.length) {\n                    lord.home = settlements[i];\n                }\n                else {\n                    lord.home = k.capital;\n                }\n                lord.moveTo(lord.home);\n                lord.enterSettlement();\n            }\n        }\n        console.log(\"Created lords: \", Lord_1.default.getLords());\n        //create map graph\n        const graph_generator = new GraphGenerator_1.default(kingdoms, WINDOW_W, WINDOW_H);\n        let graph_create_success = graph_generator.Generate(GRAPH_SEED, scene);\n        if (!graph_create_success) {\n            alert(\"The map generator has detected that some nodes are unreachable. Try generating a new map or this could have some interesting effects on the simulation!\");\n        }\n        scene.show();\n        engine.Run();\n        //start the \"game loop\"\n        const TIME_BETWEEN_REEVALUATIONS = 1000;\n        const ACTION_LOOP = setInterval(() => {\n            const lords = Lord_1.default.getLords();\n            for (let i = 0; i < lords.length; i++) {\n                lords[i].Act();\n            }\n        }, TIME_BETWEEN_REEVALUATIONS);\n    }\n    /**\n     * Gets a brand new unused settlement name\n     */\n    static getNewSettlementName() {\n        if (SETTLEMENT_NAMES.length == 0) {\n            throw new RangeError(\"Tried to get a new settlement name when the list was already exhausted.\");\n        }\n        let name = SRL_1.Utility.random.randItem(SETTLEMENT_NAMES);\n        SRL_1.Utility.array.removeItem(SETTLEMENT_NAMES, name);\n        return name;\n    }\n    /**\n     * Gets a brand new unused lord name\n     */\n    static getNewLordName() {\n        if (LORD_NAMES.length == 0) {\n            throw new RangeError(\"Tried to get a new lord name when the list was already exhausted.\");\n        }\n        let name = SRL_1.Utility.random.randItem(LORD_NAMES);\n        SRL_1.Utility.array.removeItem(LORD_NAMES, name);\n        return name;\n    }\n}\n//entry\nMain.Main();\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/index.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Bundle.ts"
/*!*******************************!*\
  !*** ./src/lib/SRL/Bundle.ts ***!
  \*******************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Bundle = void 0;\nconst Component_1 = __webpack_require__(/*! ./Component */ \"./src/lib/SRL/Component.ts\");\nclass Bundle {\n    bundled_comps = [];\n    bundleMaster = null;\n    /**\n     * Binds a list of components together. The X and Y position of higher components is determined relative to the lowest component.\n     * @param initial_components the initial bundle of components\n     */\n    constructor(initial_components) {\n        this.set(initial_components);\n    }\n    /**\n     * Adds one or more new components to the bundle\n     * @param comps the component(s) to add to the bundle\n     */\n    add(comps) {\n        let self = this;\n        if (comps instanceof Component_1.Component) {\n            parseComp(comps);\n        }\n        else {\n            comps.forEach(c => {\n                parseComp(c);\n            });\n        }\n        function parseComp(comp) {\n            self.bundled_comps.push(comp);\n            comp.bundleMaster = self.bundleMaster;\n            comp.setZ(self.bundleMaster.getZ() + self.bundled_comps.length);\n        }\n    }\n    /**\n     * Removes one or more components from the bundle\n     * @param comps the component(s) to remove from the bundle\n     */\n    remove(comps) {\n        let self = this;\n        if (comps instanceof Component_1.Component) {\n            parseComp(comps);\n        }\n        else {\n            comps.forEach(c => {\n                parseComp(c);\n            });\n        }\n        function parseComp(comp) {\n            self.bundled_comps.splice(self.bundled_comps.indexOf(comp), 1);\n            comp.setZ(comp.getZ() - comp.bundleMaster.getZ());\n            comp.bundleMaster = null;\n        }\n    }\n    /**\n     * Sets the bundle to contain the new list of components\n     * @param comps list of components in the order of rendering priority (first is lowest)\n     */\n    set(comps) {\n        this.bundled_comps = [];\n        this.bundleMaster = comps[0];\n        this.bundled_comps.push(comps[0]);\n        for (let i = 1; i < comps.length; i++) {\n            comps[i].setZ(this.bundleMaster.getZ() + i);\n            comps[i].bundleMaster = this.bundleMaster;\n            this.bundled_comps.push(comps[i]);\n        }\n    }\n    /**\n     * Enables all bundled components\n     */\n    enable() {\n        this.bundled_comps.forEach(e => {\n            e.enable();\n        });\n    }\n    /**\n     * Disables all bundled components\n     */\n    disable() {\n        this.bundled_comps.forEach(e => {\n            e.disable();\n        });\n    }\n    /**\n     * Returns a list containing the components in this bundle\n     * @returns the list of components in the bundle\n     */\n    getBundledComps() {\n        return this.bundled_comps;\n    }\n    /**\n     * Gets the bundle master of the bundle (the component the others components position is relative to)\n     * @returns the bundle master\n     */\n    getMaster() {\n        return this.bundleMaster;\n    }\n}\nexports.Bundle = Bundle;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Bundle.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Component.ts"
/*!**********************************!*\
  !*** ./src/lib/SRL/Component.ts ***!
  \**********************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Component = void 0;\nconst Vector2_1 = __webpack_require__(/*! ./Vector2 */ \"./src/lib/SRL/Vector2.ts\");\nclass Component {\n    location = new Vector2_1.Vector2(0, 0);\n    dimensions = new Vector2_1.Vector2(0, 0);\n    z = 0;\n    enabled = true;\n    bundleMaster = null;\n    UID = crypto.randomUUID();\n    static = false;\n    opacity = 1;\n    /**\n     * Abstract class represents a renderable engine component.\n     * @param scene Optional - automatically adds the new component to a given Scene.\n     */\n    constructor(scene) {\n        scene ? scene.add(this) : undefined;\n    }\n    /**\n     * Sets the location of the component\n     * @param point Location point\n     */\n    setLocation(point) {\n        this.location = point;\n    }\n    /**\n     * Gets the location of the component\n     * @returns the location\n     */\n    getLocation() {\n        return this.location;\n    }\n    /**\n     * Sets the new dimensions for the component\n     * @param dims the new dimensions for the component\n     */\n    setDimensions(dims) {\n        this.dimensions = dims;\n    }\n    /**\n     * Gets the component's dimensions\n     * @returns the component's dimensions\n     */\n    getDimensions() {\n        return this.dimensions;\n    }\n    /**\n     * Sets the opacity (transparentness) of the component\n     * @param opacity the new opacity of the component between 0-1 inclusive\n     */\n    setOpacity(opacity) {\n        if (opacity >= 0 && opacity <= 1) {\n            this.opacity = opacity;\n        }\n    }\n    /**\n     * Gets the opacity (transparentness) of the component\n     * @returns the component's opacity\n     */\n    getOpacity() {\n        return this.opacity;\n    }\n    /**\n     * Computes the component centrepoint pixel location in 2d space.\n     * @returns the centrepoint of the component\n     */\n    getCentrePoint() {\n        let centreX = Math.floor(this.getLocation().x + this.getDimensions().x / 2);\n        let centreY = Math.floor(this.getLocation().y + this.getDimensions().y / 2);\n        return new Vector2_1.Vector2(centreX, centreY);\n    }\n    /**\n     * Determines if the given point falls within the bounds of the component\n     * @param point the point to check\n     */\n    isPointInComponent(point, cameraPos) {\n        let x = this.bundleMaster ? this.location.x + this.bundleMaster.getLocation().x : this.location.x;\n        let y = this.bundleMaster ? this.location.y + this.bundleMaster.getLocation().y : this.location.y;\n        x = this.static ? x + cameraPos.x : x;\n        y = this.static ? y + cameraPos.y : y;\n        return (x <= point.x) && (point.x <= x + this.dimensions.x) && (y <= point.y) && (point.y <= y + this.dimensions.y);\n    }\n    collision(component) {\n        let out = [];\n        let self = this;\n        if (component instanceof Array) { //array of Components\n            component.forEach(comp => {\n                if (detectCol(comp)) {\n                    out.push(comp);\n                }\n            });\n        }\n        else { //solitary Component\n            if (detectCol(component)) {\n                out.push(component);\n            }\n        }\n        function detectCol(instance) {\n            let sOffsetX = self.bundleMaster ? self.bundleMaster.getLocation().x : 0;\n            let sOffsetY = self.bundleMaster ? self.bundleMaster.getLocation().y : 0;\n            let cOffsetX = instance.bundleMaster ? instance.bundleMaster.getLocation().x : 0;\n            let cOffsetY = instance.bundleMaster ? instance.bundleMaster.getDimensions().y : 0;\n            return (self.location.x + sOffsetX < instance.location.x + cOffsetX + instance.getDimensions().x) && (self.location.x + sOffsetX + self.dimensions.x > instance.location.x + cOffsetX) && (self.location.y + sOffsetY < instance.location.y + cOffsetY + instance.getDimensions().y) && (self.location.y + sOffsetY + self.dimensions.y > instance.location.y + cOffsetY);\n        }\n        return out;\n    }\n    /**\n     * Enables the component\n     */\n    enable() {\n        this.enabled = true;\n    }\n    /**\n     * Disables the component (includes rendering and calls to Update() methods)\n     */\n    disable() {\n        this.enabled = false;\n    }\n    /**\n     * Changes the z-index of the component, which affects the layer the component is rendered to (also affects events)\n     * @param zIndex the z-index to set the component to\n     */\n    setZ(zIndex) {\n        this.z = zIndex;\n    }\n    /**\n     * Gets the z-index of the component\n     * @returns the z-index\n     */\n    getZ() {\n        return this.z;\n    }\n    /**\n     * Gets the unique ID of the component\n     * @returns the UID of the component\n     */\n    toString() {\n        return this.UID;\n    }\n}\nexports.Component = Component;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Component.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Engine.ts"
/*!*******************************!*\
  !*** ./src/lib/SRL/Engine.ts ***!
  \*******************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Engine = void 0;\nconst Component_1 = __webpack_require__(/*! ./Component */ \"./src/lib/SRL/Component.ts\");\nconst Vector2_1 = __webpack_require__(/*! ./Vector2 */ \"./src/lib/SRL/Vector2.ts\");\nclass Engine {\n    container;\n    cameraPosition = new Vector2_1.Vector2(0, 0);\n    element = null;\n    engineH = null;\n    engineW = null;\n    FPSCap = null;\n    FPSLastCheck = 0;\n    LastFrameTime = 0;\n    ctx = null;\n    fixedUpdateInterval = 10;\n    mousePoint = new Vector2_1.Vector2(0, 0);\n    renderComponents = [];\n    zComponents = [];\n    mouse_down_components = [];\n    mouse_up_components = [];\n    scroll_components = [];\n    keydown_components = [];\n    keyup_components = [];\n    stopped = false;\n    EngineStatusInfo = {\n        FPS: 0,\n        FPSTotal: 0,\n        engine: this,\n        fixedInterval: this.fixedUpdateInterval\n    };\n    constructor(aContianer) {\n        this.container = aContianer;\n    }\n    /**\n     * Passes initiation information to the engine, and applies the effects.\n     */\n    Init(options) {\n        this.engineH = options.height;\n        this.engineW = options.width;\n        this.FPSCap = options.FPSCap ?? null;\n        this.element = document.createElement(\"canvas\");\n        this.element.height = this.engineH;\n        this.element.width = this.engineW;\n        this.element.className = \"SJRL2D_App\";\n        this.element.oncontextmenu = function () {\n            return false;\n        };\n        //set elem background color\n        this.element.style.backgroundColor = options.background ?? \"transparent\";\n        this.container.appendChild(this.element);\n        this.ctx = this.element.getContext(\"2d\");\n        //mouse position\n        this.element.addEventListener(\"mousemove\", this.mousemove);\n        this.element.addEventListener(\"mouseleave\", this.mouseleave);\n        //=========================================================================================================\n        //events\n        //mousedown\n        this.element.addEventListener(\"mousedown\", this.mousedown);\n        //mouseup\n        this.element.addEventListener(\"mouseup\", this.mouseup);\n        //scroll\n        document.addEventListener(\"wheel\", this.wheel);\n        //keydown\n        document.addEventListener(\"keydown\", this.keydown);\n        //keyup\n        document.addEventListener(\"keyup\", this.keyup);\n    }\n    /**\n     * Event listener functions\n     */\n    mousemove = (e) => {\n        let rect = this.element.getBoundingClientRect();\n        this.mousePoint.x = e.x - rect.x + this.cameraPosition.x;\n        this.mousePoint.y = e.y - rect.y + this.cameraPosition.y;\n    };\n    mouseleave = (e) => {\n        this.mousePoint.x = -Infinity;\n        this.mousePoint.y = -Infinity;\n    };\n    mousedown = (e) => {\n        for (let i = this.mouse_down_components.length - 1; i >= 0; i--) {\n            let comp = this.mouse_down_components[i];\n            if (comp.enabled && this.isComponentWithinCamera(comp)) {\n                if (comp.isPointInComponent(this.mousePoint, this.cameraPosition)) {\n                    comp.MouseDown(e);\n                    break;\n                }\n            }\n        }\n    };\n    mouseup = (e) => {\n        for (let i = this.mouse_up_components.length - 1; i >= 0; i--) {\n            let comp = this.mouse_up_components[i];\n            if (comp.enabled && this.isComponentWithinCamera(comp)) {\n                if (comp.isPointInComponent(this.mousePoint, this.cameraPosition)) {\n                    comp.MouseUp(e);\n                    break;\n                }\n            }\n        }\n    };\n    wheel = (e) => {\n        for (let i = 0; i < this.scroll_components.length; i++) {\n            let comp = this.scroll_components[i];\n            if (comp.enabled) {\n                comp.ScrollWheel(e);\n            }\n        }\n    };\n    keydown = (e) => {\n        for (let i = 0; i < this.keydown_components.length; i++) {\n            let comp = this.keydown_components[i];\n            if (comp.enabled) {\n                comp.KeyDown(e);\n            }\n        }\n    };\n    keyup = (e) => {\n        for (let i = 0; i < this.keyup_components.length; i++) {\n            let comp = this.keyup_components[i];\n            if (comp.enabled) {\n                comp.KeyUp(e);\n            }\n        }\n    };\n    /**\n     * Runs the rendering process\n     */\n    Run() {\n        let self = this;\n        window.requestAnimationFrame(() => {\n            this.loop(this);\n        });\n        //FixedUpdate methods\n        triggerFixedUpdate();\n        function triggerFixedUpdate() {\n            self.renderComponents.forEach(comp => {\n                if (comp.FixedUpdate && comp.enabled) {\n                    comp.FixedUpdate(self.EngineStatusInfo);\n                }\n            });\n            if (!self.stopped) {\n                setTimeout(triggerFixedUpdate, self.fixedUpdateInterval);\n            }\n        }\n        let fps_interval = setInterval(() => {\n            this.FPSChecker(this);\n            if (this.stopped) {\n                clearInterval(fps_interval);\n            }\n        }, 1000);\n    }\n    /**\n     * Stops the engine from running, cannot be started.\n     */\n    Stop() {\n        this.stopped = true;\n        this.element.removeEventListener(\"mousedown\", this.mousedown);\n        this.element.removeEventListener(\"mouseup\", this.mouseup);\n        this.element.removeEventListener(\"mousemove\", this.mousemove);\n        this.element.removeEventListener(\"mouseleave\", this.mouseleave);\n        document.removeEventListener(\"wheel\", this.wheel);\n        document.removeEventListener(\"keydown\", this.keydown);\n        document.removeEventListener(\"keyup\", this.keyup);\n    }\n    /**\n     * Removes the app from the page, and stops its processes if required\n     */\n    Remove() {\n        if (!this.stopped) {\n            this.Stop();\n        }\n        this.element.remove();\n    }\n    loop(self) {\n        let current_ms = window.performance.now();\n        let desired_ms_per_frame = (1000 / self.FPSCap);\n        let elapsed_since_last_call = current_ms - self.LastFrameTime;\n        let tempz = [];\n        if (typeof (self.FPSCap) !== \"number\" || (elapsed_since_last_call >= desired_ms_per_frame)) {\n            this.EngineStatusInfo.FPSTotal++;\n            let ctx = self.ctx;\n            //run component's update method before frame draw\n            this.renderComponents.forEach((e) => {\n                if (e.Update && e.enabled) {\n                    e.Update(self.EngineStatusInfo);\n                }\n                tempz.push(e.UID + e.getZ() + e.getLocation().x + e.getLocation().y + e.getDimensions().x + e.getDimensions().y);\n            });\n            //clear canvas\n            ctx.clearRect(0, 0, self.engineW, self.engineH);\n            //draw the frame\n            if (tempz.toString() != this.zComponents.toString()) {\n                this.sortComponents();\n                this.zComponents = tempz;\n            }\n            this.renderComponents.forEach((e) => {\n                if (e.enabled && this.isComponentWithinCamera(e)) {\n                    e.Renderer.Render(ctx, e, this.cameraPosition);\n                }\n            });\n            //late updates\n            this.renderComponents.forEach((e) => {\n                if (e.LateUpdate && e.enabled) {\n                    e.LateUpdate(self.EngineStatusInfo);\n                }\n            });\n            //makes sure any excess ms that has elapsed since the minimum time waited for new frame to be drawn is accounted for\n            self.LastFrameTime = current_ms - (elapsed_since_last_call % desired_ms_per_frame);\n        }\n        window.requestAnimationFrame(() => {\n            if (!this.stopped) {\n                self.loop(self);\n            }\n        });\n    }\n    /**\n     * Starts on 1000ms interval from when Run() is run. Checks the number of frames drawn since 1 second ago, and updates information in EngineStatusInfo accordingly.\n     */\n    FPSChecker(self) {\n        self.EngineStatusInfo.FPS = (this.EngineStatusInfo.FPSTotal - self.FPSLastCheck);\n        self.FPSLastCheck = this.EngineStatusInfo.FPSTotal;\n    }\n    /**\n     * Sets the number of milliseconds interval between each component's FixedUpdate() method is called.\n     * @param ms ms per update\n     */\n    setFixedUpdateInterval(ms) {\n        this.fixedUpdateInterval = ms;\n        this.EngineStatusInfo.fixedInterval = ms;\n    }\n    /**\n     * Returns the mouse's location in Point format, null if the mouse is not currently on the canvas.\n     * @returns the Point of the mouse if it's on the canvas, else null\n     */\n    getMousePoint() {\n        return this.mousePoint;\n    }\n    /**\n     * Gets the canvas element the engine is using\n     * @returns the canvas element\n     */\n    getElement() {\n        return this.element;\n    }\n    /**\n     * Determines if the component is within the bounds of the camera, or not\n     * @param comp the component to check\n     * @returns true if the component is within the bounds of the camera, else false\n     */\n    isComponentWithinCamera(comp) {\n        if (comp.static) { //static components are always rendered\n            return true;\n        }\n        let x = comp.bundleMaster ? comp.getLocation().x + comp.bundleMaster.getLocation().x : comp.getLocation().x;\n        let y = comp.bundleMaster ? comp.getLocation().y + comp.bundleMaster.getLocation().y : comp.getLocation().y;\n        if ((x + comp.getDimensions().x - this.cameraPosition.x < 0) || //off sceen left\n            (y - this.cameraPosition.x > this.engineW) || //off screen right\n            (x + comp.getDimensions().y - this.cameraPosition.y < 0) || //off screen top\n            (y - this.cameraPosition.y > this.engineH) //off screen bottom\n        ) {\n            return false;\n        }\n        return true;\n    }\n    /**\n     * Adds a component or a list of components to the render list, where they will start being rendered\n     * @param comp the component or list of components to add to the render list\n     */\n    addToRender(comp) {\n        let self = this;\n        if (comp instanceof Component_1.Component) {\n            parseComp(comp);\n        }\n        else {\n            comp.forEach(c => {\n                parseComp(c);\n            });\n        }\n        function parseComp(component) {\n            self.renderComponents.push(component);\n            if (typeof (component.MouseDown) === \"function\") { //if component has a mousedown event\n                self.mouse_down_components.push(component);\n            }\n            if (typeof (component.MouseUp) === \"function\") { //if component has a mouseup event\n                self.mouse_up_components.push(component);\n            }\n            if (typeof (component.ScrollWheel) === \"function\") {\n                self.scroll_components.push(component);\n            }\n            if (typeof (component.KeyDown) === \"function\") {\n                self.keydown_components.push(component);\n            }\n            if (typeof (component.KeyUp) === \"function\") {\n                self.keyup_components.push(component);\n            }\n        }\n    }\n    /**\n     * Removes a component or a list of components from the render list\n     * @param comp the component or list of components to be removed from the render list\n     */\n    removeFromRender(comp) {\n        let self = this;\n        if (comp instanceof Component_1.Component) {\n            if (this.renderComponents.includes(comp)) {\n                parseComp(comp);\n            }\n        }\n        else {\n            comp.forEach(c => {\n                if (this.renderComponents.includes(c)) {\n                    parseComp(c);\n                }\n            });\n        }\n        function parseComp(component) {\n            self.renderComponents.splice(self.renderComponents.indexOf(component), 1);\n            if (typeof (component.MouseDown) === \"function\") { //if component has a mousedown event\n                self.renderComponents.splice(self.renderComponents.indexOf(component), 1);\n            }\n            if (typeof (component.MouseUp) === \"function\") { //if component has a mouseup event\n                self.renderComponents.splice(self.renderComponents.indexOf(component), 1);\n            }\n            if (typeof (component.ScrollWheel) === \"function\") {\n                self.renderComponents.splice(self.renderComponents.indexOf(component), 1);\n            }\n            if (typeof (component.KeyDown) === \"function\") {\n                self.renderComponents.splice(self.renderComponents.indexOf(component), 1);\n            }\n            if (typeof (component.KeyUp) === \"function\") {\n                self.renderComponents.splice(self.renderComponents.indexOf(component), 1);\n            }\n        }\n    }\n    /**\n     * Sorts the components list by z-index\n     */\n    sortComponents() {\n        //all\n        this.renderComponents = this.renderComponents.sort(function (a, b) {\n            return a.getZ() - b.getZ();\n        });\n        //mousedown\n        this.mouse_down_components = this.mouse_down_components.sort(function (a, b) {\n            return a.getZ() - b.getZ();\n        });\n        //mouseup\n        this.mouse_up_components = this.mouse_up_components.sort(function (a, b) {\n            return a.getZ() - b.getZ();\n        });\n    }\n    /**\n     * Returns information about the engine instance\n     * @returns the engine's engine info object\n     */\n    getEngineInfo() {\n        return this.EngineStatusInfo;\n    }\n    /**\n     * Gets the current position of the \"camera\"\n     * @returns the current camera position\n     */\n    getCameraPos() {\n        return this.cameraPosition;\n    }\n    /**\n     * Sets the \"camera\" position\n     * @param position the new position of the camera\n     */\n    setCameraPos(position) {\n        this.cameraPosition = position;\n    }\n    /**\n     * Gets the engines rendering context\n     * @returns the engines rendering context\n     */\n    getRenderingContext() {\n        return this.ctx;\n    }\n}\nexports.Engine = Engine;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Engine.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Grid.ts"
/*!*****************************!*\
  !*** ./src/lib/SRL/Grid.ts ***!
  \*****************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Grid = void 0;\nconst Bundle_1 = __webpack_require__(/*! ./Bundle */ \"./src/lib/SRL/Bundle.ts\");\nconst Component_1 = __webpack_require__(/*! ./Component */ \"./src/lib/SRL/Component.ts\");\nconst Renderers_1 = __webpack_require__(/*! ./Renderers */ \"./src/lib/SRL/Renderers.ts\");\nconst Vector2_1 = __webpack_require__(/*! ./Vector2 */ \"./src/lib/SRL/Vector2.ts\");\n/**\n * Grid class groups a number of components, bundles them, and displays them in a gridular format. The location of the top left cell determines the location of each cell.\n */\nclass Grid extends Bundle_1.Bundle {\n    grid_dimensions = new Vector2_1.Vector2(0, 0);\n    cell_dimensions = new Vector2_1.Vector2(0, 0);\n    /**\n     * @param initial_components components to be immediately loaded into the grid\n     * @param grid_dimensions the height and width of the grid in cells\n     * @param cell_dimensions the hight and width of the cells\n     */\n    constructor(initial_components, grid_dimensions, cell_dimensions) {\n        super(initial_components);\n        this.setCellDimensions(cell_dimensions);\n        this.setGridDimensions(grid_dimensions);\n        for (let i = 0; i < initial_components.length; i++) {\n            let comp = initial_components[i];\n            comp.setDimensions(cell_dimensions);\n            comp.getLocation().x = (i % grid_dimensions.x) * cell_dimensions.x;\n            comp.getLocation().y = Math.floor(i / grid_dimensions.x) * cell_dimensions.y;\n            comp.setZ(initial_components[0].getZ());\n        }\n    }\n    /**\n     * EmptyCell can be used to set a grid's cell to be empty. Create a new instance and pass it to the grid. Inherits Component\n     */\n    static EmptyCell = class extends Component_1.Component {\n        constructor(scene) {\n            super(scene);\n        }\n        Renderer = new Renderers_1.NullRenderer();\n    };\n    /**\n     * Changes the grid location\n     * @param location the new location of the grid\n     */\n    setLocation(location) {\n        this.getMaster().setLocation(location);\n    }\n    /**\n     * Sets the new high and widh of the grid in cells (must be > 0)\n     * @param grid_dimensions the new height and width of the grid in cells\n     * @returns true if the operation was successful, else false\n     */\n    setGridDimensions(grid_dimensions) {\n        if (grid_dimensions.x > 0 && grid_dimensions.y > 0) {\n            this.grid_dimensions = grid_dimensions;\n            return true;\n        }\n        return false;\n    }\n    /**\n     * Sets the new size of each of the cells in the grid (must be > 0)\n     * @param cell_dimensions the new dimensions of the cells in the grid\n     * @returns true if the operation was successful, else false\n     */\n    setCellDimensions(cell_dimensions) {\n        if (cell_dimensions.x > 0 && cell_dimensions.y > 0) {\n            this.cell_dimensions = cell_dimensions;\n            return true;\n        }\n        return false;\n    }\n    /**\n     * Places a new component within a cell in the grid\n     * @param cell X and Y of the cell to be replaced\n     * @param component The component to show in the cell\n     * @returns true if the operation was successful, else false\n     */\n    setCellAt(cell, component) {\n        if (cell.x < 0 || cell.y < 0 || cell.x >= this.grid_dimensions.x || cell.y >= this.cell_dimensions.x) {\n            return false;\n        }\n        component.setDimensions(this.cell_dimensions);\n        component.getLocation().x = cell.x * this.cell_dimensions.x;\n        component.getLocation().y = cell.y * this.cell_dimensions.y;\n        let comps = [...this.getBundledComps()];\n        let old_comp = comps[cell.x + Math.floor(cell.y * this.grid_dimensions.x)];\n        old_comp.bundleMaster = null;\n        comps[cell.x + Math.floor(cell.y * this.grid_dimensions.x)] = component;\n        this.set(comps);\n        for (let i = 0; i < comps.length; i++) {\n            comps[i].setZ(comps[0].getZ());\n        }\n        return true;\n    }\n    /**\n     * Returns the component at the given position in the grid, else null\n     * @param location the x,y position of the component in the grid\n     * @returns the component in the given position in the grid.\n     */\n    getCellAt(location) {\n        let comp;\n        try {\n            comp = this.getBundledComps()[location.x + Math.floor(location.y * this.grid_dimensions.x)];\n        }\n        catch (err) {\n            return null;\n        }\n        return comp;\n    }\n}\nexports.Grid = Grid;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Grid.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Renderers.ts"
/*!**********************************!*\
  !*** ./src/lib/SRL/Renderers.ts ***!
  \**********************************/
(__unused_webpack_module, exports) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.LineRenderer = exports.AnimationRenderer = exports.TextRenderer = exports.SpriteRenderer = exports.SolidRenderer = exports.NullRenderer = void 0;\nclass NullRenderer {\n    Render(ctx, component, cameraPos) {\n        return;\n    }\n}\nexports.NullRenderer = NullRenderer;\nclass SolidRenderer {\n    color = \"red\";\n    constructor(aColor) {\n        this.color = aColor;\n    }\n    setColor(color) {\n        this.color = color;\n    }\n    Render(ctx, component, cameraPos) {\n        ctx.globalAlpha = component.getOpacity();\n        ctx.fillStyle = this.color;\n        let x = component.bundleMaster ? component.getLocation().x + component.bundleMaster.getLocation().x : component.getLocation().x;\n        let y = component.bundleMaster ? component.getLocation().y + component.bundleMaster.getLocation().y : component.getLocation().y;\n        x = component.static ? x : x - cameraPos.x;\n        y = component.static ? y : y - cameraPos.y;\n        ctx.fillRect(x, y, component.getDimensions().x, component.getDimensions().y);\n    }\n}\nexports.SolidRenderer = SolidRenderer;\nclass SpriteRenderer {\n    sprite;\n    rotation = 0;\n    constructor(sprite) {\n        this.sprite = sprite;\n    }\n    setSprite(sprite) {\n        this.sprite = sprite;\n    }\n    setRotation(angle) {\n        this.rotation = angle;\n    }\n    Render(ctx, component, cameraPos) {\n        ctx.globalAlpha = component.getOpacity();\n        let x = component.bundleMaster ? component.getLocation().x + component.bundleMaster.getLocation().x : component.getLocation().x;\n        let y = component.bundleMaster ? component.getLocation().y + component.bundleMaster.getLocation().y : component.getLocation().y;\n        x = component.static ? x : x - cameraPos.x;\n        y = component.static ? y : y - cameraPos.y;\n        if (this.rotation !== 0) {\n            ctx.save();\n            ctx.translate(component.getLocation().x + component.getDimensions().x / 2, component.getLocation().y + component.getDimensions().y / 2);\n            ctx.rotate(this.rotation * Math.PI / 180);\n            ctx.drawImage(this.sprite.drawnSprite, -component.getDimensions().x / 2, -component.getDimensions().y / 2, component.getDimensions().x, component.getDimensions().y);\n            ctx.restore();\n        }\n        else {\n            ctx.drawImage(this.sprite.drawnSprite, x, y, component.getDimensions().x, component.getDimensions().y);\n        }\n    }\n}\nexports.SpriteRenderer = SpriteRenderer;\nclass TextRenderer {\n    textmetric = null;\n    Render(ctx, component, cameraPos) {\n        ctx.globalAlpha = component.getOpacity();\n        ctx.font = component.getFont();\n        ctx.textAlign = component.align;\n        ctx.textBaseline = component.baseline_align;\n        ctx.direction = component.direction;\n        let x = component.bundleMaster ? component.getLocation().x + component.bundleMaster.getLocation().x : component.getLocation().x;\n        let y = component.bundleMaster ? component.getLocation().y + component.bundleMaster.getLocation().y : component.getLocation().y;\n        x = component.static ? x : x - cameraPos.x;\n        y = component.static ? y : y - cameraPos.y;\n        if (component.getRenderingStyle() === \"fill\") { //fill\n            ctx.fillStyle = component.style;\n            ctx.fillText(component.getText(), x, y, component.getMaxWidth());\n        }\n        else { //stroke\n            ctx.strokeStyle = component.style;\n            ctx.strokeText(component.getText(), x, y, component.getMaxWidth());\n        }\n        //update h/w if required\n        let new_metrics = ctx.measureText(component.getText());\n        if ((this.textmetric === null) || ((new_metrics.width !== this.textmetric.width) || (new_metrics.actualBoundingBoxDescent - new_metrics.actualBoundingBoxAscent !== this.textmetric.actualBoundingBoxDescent - this.textmetric.actualBoundingBoxAscent))) {\n            this.textmetric = new_metrics;\n            component.getDimensions().x = new_metrics.width;\n            component.getDimensions().y = new_metrics.actualBoundingBoxDescent - new_metrics.actualBoundingBoxAscent;\n        }\n    }\n}\nexports.TextRenderer = TextRenderer;\nclass LineRenderer {\n    Render(ctx, component, cameraPos) {\n        let x1 = component.bundleMaster ? component.getLineBegin().x + component.bundleMaster.getLocation().x : component.getLineBegin().x;\n        let y1 = component.bundleMaster ? component.getLineBegin().y + component.bundleMaster.getLocation().y : component.getLineBegin().y;\n        x1 = component.static ? x1 : x1 - cameraPos.x;\n        y1 = component.static ? y1 : y1 - cameraPos.y;\n        let x2 = component.bundleMaster ? component.getLineEnd().x + component.bundleMaster.getLocation().x : component.getLineEnd().x;\n        let y2 = component.bundleMaster ? component.getLineEnd().y + component.bundleMaster.getLocation().y : component.getLineEnd().y;\n        x2 = component.static ? x2 : x2 - cameraPos.x;\n        y2 = component.static ? y2 : y2 - cameraPos.y;\n        ctx.lineWidth = component.getLineWidth();\n        ctx.strokeStyle = component.getColour();\n        ctx.lineCap = component.getCap();\n        ctx.setLineDash([component.getLineDashLength(), component.getLineGapLength()]);\n        ctx.beginPath();\n        ctx.moveTo(x1, y1);\n        ctx.lineTo(x2, y2);\n        ctx.stroke();\n    }\n}\nexports.LineRenderer = LineRenderer;\nclass AnimationRenderer {\n    sprite_renderer_list = [];\n    loop = false;\n    type = 0; //0 is frame based, 1 is time based\n    interval_obj = null;\n    interval = 1;\n    engine_info;\n    startFrameNo = 0;\n    playing = false;\n    paused = false;\n    current_sprite_index = 0;\n    /**\n     *\n     * @param params\n     */\n    constructor(params) {\n        if (params.sprites) {\n            for (let i = 0; i < params.sprites.length; i++) {\n                this.sprite_renderer_list.push(new SpriteRenderer(params.sprites[i]));\n            }\n        }\n        if (typeof (params.interval) == \"number\") {\n            this.Interval(params.interval);\n        }\n        if (typeof (params.loop) == \"boolean\") {\n            this.Loop(params.loop);\n        }\n        if (typeof (params.type) == \"number\") {\n            if (params.type == 0 || params.type == 1) {\n                this.type = params.type;\n            }\n            else {\n                this.type = 0;\n            }\n        }\n        this.engine_info = params.engine.getEngineInfo();\n    }\n    /**\n     * Starts the animation\n     */\n    Start() {\n        if (this.type == 0) { //frames\n            this.startFrameNo = this.engine_info.FPSTotal;\n        }\n        else { //time\n            this.interval_obj = setInterval(() => {\n                this.current_sprite_index++;\n                if (this.current_sprite_index == this.sprite_renderer_list.length) {\n                    if (this.loop) {\n                        this.current_sprite_index = 0;\n                    }\n                    else {\n                        this.current_sprite_index--;\n                        clearInterval(this.interval_obj);\n                    }\n                }\n            }, this.interval);\n        }\n        this.playing = true;\n        this.current_sprite_index = 0;\n    }\n    /**\n     * Pauses/Unpauses the animation\n     */\n    Pause() {\n        if (!this.paused) {\n            this.paused = true;\n            if (this.type == 1) {\n                clearInterval(this.interval_obj);\n            }\n        }\n        else {\n            this.paused = false;\n            if (this.type == 1) {\n                this.interval_obj = setInterval(() => {\n                    this.current_sprite_index++;\n                    if (this.current_sprite_index == this.sprite_renderer_list.length) {\n                        if (this.loop) {\n                            this.current_sprite_index = 0;\n                        }\n                        else {\n                            this.current_sprite_index--;\n                            clearInterval(this.interval_obj);\n                        }\n                    }\n                }, this.interval);\n            }\n        }\n    }\n    /**\n     * Unpauses the animation\n     */\n    Unpause() {\n        if (this.paused) {\n            this.paused = false;\n            if (this.type == 1) {\n                this.interval_obj = setInterval(() => {\n                    this.current_sprite_index++;\n                    if (this.current_sprite_index == this.sprite_renderer_list.length) {\n                        if (this.loop) {\n                            this.current_sprite_index = 0;\n                        }\n                        else {\n                            this.current_sprite_index--;\n                            clearInterval(this.interval_obj);\n                        }\n                    }\n                }, this.interval);\n            }\n        }\n    }\n    /**\n     * Stops the animation and reverts to the first sprite\n     */\n    Stop() {\n        this.current_sprite_index = 0;\n        this.playing = false;\n        this.paused = false;\n        clearInterval(this.interval_obj);\n    }\n    /**\n     * Sets if the animation should loop or play once\n     * @param doesLoop if the animation should loop or not\n     */\n    Loop(doesLoop) {\n        this.loop = doesLoop;\n    }\n    /**\n     * Sets the number of frames between each sprite change (minimum of 1)\n     * @param int frames between each sprite change\n     */\n    Interval(int) {\n        if (int > 0) {\n            this.interval = int;\n        }\n        else {\n            this.interval = 1;\n        }\n    }\n    Render(ctx, component, cameraPos) {\n        ctx.globalAlpha = component.getOpacity();\n        if (this.type == 0 && this.playing && !this.paused) {\n            if (this.startFrameNo !== this.engine_info.FPSTotal && ((this.engine_info.FPSTotal - this.startFrameNo) % this.interval === 0)) { //next sprite\n                this.current_sprite_index++;\n                if (this.current_sprite_index == this.sprite_renderer_list.length) { //end of animation\n                    if (this.loop) {\n                        this.current_sprite_index = 0;\n                        this.startFrameNo = this.engine_info.FPSTotal;\n                    }\n                    else {\n                        this.playing = false;\n                        this.current_sprite_index--;\n                    }\n                }\n            }\n        }\n        else if (this.type == 0 && this.paused) {\n            this.startFrameNo++;\n        }\n        this.sprite_renderer_list[this.current_sprite_index].Render(ctx, component, cameraPos);\n    }\n}\nexports.AnimationRenderer = AnimationRenderer;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Renderers.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Scene.ts"
/*!******************************!*\
  !*** ./src/lib/SRL/Scene.ts ***!
  \******************************/
(__unused_webpack_module, exports) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Scene = void 0;\n/**\n * A \"Scene\", groups instances of objects that conform to IEngineRenderable and performs desired operations on all at once\n */\nclass Scene {\n    batchedComponents = [];\n    engine;\n    showing = false;\n    constructor(anEngine) {\n        this.engine = anEngine;\n    }\n    /**\n     * Adds a single Component to the batch (duplicates will fail)\n     * @param component a component to add to the batch\n     * @returns true if component successfully added, else false\n     */\n    add(component) {\n        if (this.contains(component)) {\n            return false;\n        }\n        this.batchedComponents.push(component);\n        if (component.enabled && this.showing) {\n            this.engine.addToRender(component);\n        }\n        return true;\n    }\n    /**\n     * Adds many Component to the batch (duplicates will fail)\n     * @param components array of components to be added to the batch\n     * @returns true if all components successfully added, else false\n     */\n    addMany(components) {\n        let out = true;\n        components.forEach((e) => {\n            if (!this.add(e)) {\n                out = false;\n            }\n        });\n        return out;\n    }\n    /**\n     * Removes a single component from the batch, if it exists in the batch\n     * @param component the component to be removed from the collection\n     * @returns true if the component is successfully removed, else false if not found.\n     */\n    remove(component) {\n        if (this.contains(component)) {\n            this.batchedComponents.splice(this.batchedComponents.indexOf(component), 1);\n            return true;\n        }\n        else {\n            return false;\n        }\n    }\n    /**\n     * Removes a group of elements from the batch (if each exists in the batch)\n     * @param components Array of components to remove from the batch\n     * @returns true if all elements are successfully removed, else false.\n     */\n    removeMany(components) {\n        let out = true;\n        components.forEach((e) => {\n            if (!this.remove(e)) {\n                out = false;\n            }\n        });\n        return out;\n    }\n    /**\n     * Checks if a component exists in the collection\n     * @param component the component to check\n     * @returns true if component is found, else false\n     */\n    contains(component) {\n        return this.batchedComponents.includes(component);\n    }\n    show() {\n        this.engine.addToRender(this.batchedComponents);\n        this.showing = true;\n    }\n    hide() {\n        this.engine.removeFromRender(this.batchedComponents);\n        this.showing = false;\n    }\n}\nexports.Scene = Scene;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Scene.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Sprite.ts"
/*!*******************************!*\
  !*** ./src/lib/SRL/Sprite.ts ***!
  \*******************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Sprite = void 0;\nconst Vector2_1 = __webpack_require__(/*! ./Vector2 */ \"./src/lib/SRL/Vector2.ts\");\nclass Sprite {\n    source;\n    bounds = null;\n    drawnSprite;\n    constructor(img, def) {\n        this.source = img;\n        let size;\n        let offset = new Vector2_1.Vector2(0, 0);\n        if (def) {\n            this.bounds = def;\n            size = this.bounds.dimensions;\n            offset = this.bounds.location;\n        }\n        else {\n            size = new Vector2_1.Vector2(img.naturalWidth, img.naturalHeight);\n        }\n        this.drawnSprite = new OffscreenCanvas(size.x, size.y);\n        let ctx = this.drawnSprite.getContext(\"2d\");\n        ctx.drawImage(img, offset.x, offset.y, size.x, size.y, 0, 0, size.x, size.y);\n    }\n}\nexports.Sprite = Sprite;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Sprite.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Spritesheet.ts"
/*!************************************!*\
  !*** ./src/lib/SRL/Spritesheet.ts ***!
  \************************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Spritesheet = void 0;\nconst Sprite_1 = __webpack_require__(/*! ./Sprite */ \"./src/lib/SRL/Sprite.ts\");\nconst Vector2_1 = __webpack_require__(/*! ./Vector2 */ \"./src/lib/SRL/Vector2.ts\");\nclass Spritesheet {\n    sprites;\n    source;\n    constructor(img) {\n        this.source = img;\n    }\n    /**\n     * Defines a set of sprites on the spritesheet.\n     * @param spritedefs array of objects containing the sprite names, x, y, w and h locations on the spritesheet.\n     */\n    defineSprites(spritedefs) {\n        this.sprites = [];\n        for (let i = 0; i < spritedefs.length; i++) {\n            let obj = spritedefs[i];\n            this.sprites.push({ name: obj.name, object: new Sprite_1.Sprite(this.source, { location: new Vector2_1.Vector2(obj.x, obj.y), dimensions: new Vector2_1.Vector2(obj.w, obj.h) }) });\n        }\n    }\n    /**\n     * Creates sprites from the spritesheet if the sprites all have the same dimensions\n     * @param start_location top left location of the top left sprite\n     * @param dimensions height and width of the sprites\n     * @param names list of the names that are given to each of the sprites\n     * @param xGap horizontal gap between the sprites if there is any\n     * @param yGap vertical gap between the sprites if there is any\n     */\n    splitEvery(start_location, dimensions, names, xGap, yGap) {\n        this.sprites = [];\n        const startX = start_location.x;\n        const startY = start_location.y;\n        const sourceH = this.source.height;\n        const sourceW = this.source.width;\n        if (!xGap) {\n            xGap = 0;\n        }\n        if (!yGap) {\n            yGap = 0;\n        }\n        let currX = startX;\n        let currY = startY;\n        let finished = false;\n        let spriteNo = 0;\n        while (!finished) {\n            this.sprites.push({ name: names[spriteNo], object: new Sprite_1.Sprite(this.source, { location: new Vector2_1.Vector2(currX, currY), dimensions: dimensions }) });\n            currX += dimensions.x + xGap;\n            if (currX == sourceW) { //new row\n                currY += dimensions.y + yGap;\n                currX = startX;\n                if (currY == sourceH) { //fin\n                    finished = true;\n                }\n            }\n            spriteNo++;\n        }\n    }\n    /**\n     * Gets the list of sprites defined on the spritesheet\n     * @returns the list of sprites defined on the spritesheet\n     */\n    getSprites() {\n        return this.sprites;\n    }\n    /**\n     * Returns a sprite with the given name\n     * @param name the name of the sprite\n     * @returns a sprite with the given name or null if not found\n     */\n    getSprite(name) {\n        for (let i = 0; i < this.sprites.length; i++) {\n            if (this.sprites[i].name == name) {\n                return this.sprites[i].object;\n            }\n        }\n        return null;\n    }\n}\nexports.Spritesheet = Spritesheet;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Spritesheet.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/TextComponent.ts"
/*!**************************************!*\
  !*** ./src/lib/SRL/TextComponent.ts ***!
  \**************************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.TextComponent = void 0;\nconst Component_1 = __webpack_require__(/*! ./Component */ \"./src/lib/SRL/Component.ts\");\nconst Renderers_1 = __webpack_require__(/*! ./Renderers */ \"./src/lib/SRL/Renderers.ts\");\nclass TextComponent extends Component_1.Component {\n    constructor(scene) {\n        super(scene);\n    }\n    text = \"\";\n    max_width = window.outerWidth;\n    rendering_style = \"fill\";\n    font = \"10px serif\";\n    style = \"black\";\n    align = \"start\";\n    baseline_align = \"top\";\n    direction = \"inherit\";\n    /**\n     * Changes the displayed text of the component\n     * @param text text\n     */\n    setText(text) {\n        this.text = text;\n        //update dimensions\n        let ctx = document.createElement(\"canvas\").getContext(\"2d\");\n        ctx.font = this.getFont();\n        ctx.textAlign = this.align;\n        ctx.textBaseline = this.baseline_align;\n        ctx.direction = this.direction;\n        let metrics = ctx.measureText(text);\n        let w = metrics.width;\n        if (metrics.width > this.max_width) {\n            w = this.max_width;\n        }\n        this.getDimensions().x = w;\n        this.getDimensions().y = metrics.actualBoundingBoxDescent - metrics.actualBoundingBoxAscent;\n    }\n    /**\n     * Gets the currently set text of the component\n     * @returns text\n     */\n    getText() {\n        return this.text;\n    }\n    /**\n     * Sets the maximum width the text will be displayed\n     * @param width width in px\n     */\n    setMaxWidth(width) {\n        this.max_width = width;\n    }\n    /**\n     * Gets the maximum width the text will be displayed at, if already manually set.\n     * @returns the currently set max width\n     */\n    getMaxWidth() {\n        return this.max_width;\n    }\n    /**\n     * Sets the text rendering style to \"fill\" or \"stroke\" (default is \"fill\")\n     * @param style \"fill\" or \"stroke\"\n     * @returns true if the operation was successful, else false\n     */\n    setRenderingStyle(style) {\n        if (style === \"fill\" || style === \"stroke\") {\n            this.rendering_style = style;\n            return true;\n        }\n        return false;\n    }\n    getRenderingStyle() {\n        return this.rendering_style;\n    }\n    /**\n     * Defines the font the text will adhere to.\n     * @param font the string that defines the font\n     */\n    setFont(font) {\n        this.font = font;\n    }\n    /**\n     * Gets the currently set font for the component\n     * @returns font\n     */\n    getFont() {\n        return this.font;\n    }\n    Renderer = new Renderers_1.TextRenderer();\n}\nexports.TextComponent = TextComponent;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/TextComponent.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Utility.ts"
/*!********************************!*\
  !*** ./src/lib/SRL/Utility.ts ***!
  \********************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Utility = void 0;\nconst Vector2_1 = __webpack_require__(/*! ./Vector2 */ \"./src/lib/SRL/Vector2.ts\");\n//========================================Random================================================================\n/**\n * Returns an unbiased integer between max and min\n * @param min minimum value\n * @param max maximum value\n * @param include_max maximum value is inclusive\n * @returns random integer\n */\nfunction randInt(min, max, include_max) {\n    if (include_max) {\n        return Math.floor(Math.random() * (max - min + 1)) + min;\n    }\n    return Math.floor(Math.random() * (max - min)) + min;\n}\n/**\n * Generates a random biased integer, with regards to a supplied mean and standard deviation\n * @param min minimum possible value (inclusive)\n * @param max maximum possible value (exclusive)\n * @param mean average value\n * @param standard_deviation how \"wide\" the curve will be\n * @returns a biased random integer\n */\nfunction randGaussianInt(min, max, mean, standard_deviation = 1) {\n    /**\n     * As defined by Tom Liao (2019) https://spin.atomicobject.com/2019/09/30/skew-normal-prng-javascript/\n     */\n    let u1 = 0, u2 = 0;\n    while (u1 === 0)\n        u1 = Math.random();\n    while (u2 === 0)\n        u2 = Math.random();\n    let r = Math.sqrt(-2 * Math.log(u1));\n    let O = 2 * Math.PI * u2;\n    let r1 = r * Math.cos(O);\n    let out = Math.ceil(mean + (standard_deviation * r1));\n    if (out < min || out > max) {\n        return randGaussianInt(min, max, mean, standard_deviation);\n    }\n    return out;\n}\n/**\n * Returns a random item in the given list\n * @param list the list\n * @returns random item in the list\n */\nfunction randItem(list) {\n    return list[randInt(0, list.length)];\n}\n/**\n * Returns a random index number of the list\n * @param list the list\n * @returns a random index of the list\n */\nfunction randIndex(list) {\n    return randInt(0, list.length);\n}\nclass SeededRandomNG {\n    a;\n    b;\n    c;\n    d;\n    lower = 0;\n    upper = 1;\n    /**\n     * Creates a new instance of a seeded random number generator.\n     * @param seed the seed\n     */\n    constructor(seed) {\n        let hash = this.cryb128(seed);\n        this.a = hash[0];\n        this.b = hash[1];\n        this.c = hash[2];\n        this.d = hash[3];\n        this.sfc32();\n    }\n    /**\n     * Takes the hash and generates the next pseudorandom number\n     * @returns new pseudorandom number\n     */\n    sfc32() {\n        this.a |= 0;\n        this.b |= 0;\n        this.c |= 0;\n        this.d |= 0;\n        let t = (this.a + this.b | 0) + this.d | 0;\n        this.d = this.d + 1 | 0;\n        this.a = this.b ^ this.b >>> 9;\n        this.b = this.c + (this.c << 3) | 0;\n        this.c = this.c + t | 0;\n        return (t >>> 0) / 4294967296;\n    }\n    /**\n     * Takes the initial seed and creates a hash from it.\n     * @param str initial seed\n     * @returns the hash\n     */\n    cryb128(str) {\n        let h1 = 1779033703;\n        let h2 = 3144134277;\n        let h3 = 1013904242;\n        let h4 = 2773480762;\n        for (let i = 0; i < str.length; i++) {\n            let k = str.charCodeAt(i);\n            h1 = h2 ^ Math.imul(h1 ^ k, 597399067);\n            h2 = h3 ^ Math.imul(h2 ^ k, 2869860233);\n            h3 = h4 ^ Math.imul(h3 ^ k, 951274213);\n            h4 = h1 ^ Math.imul(h4 ^ k, 2716044179);\n        }\n        h1 = Math.imul(h3 ^ (h1 >>> 18), 597399067);\n        h2 = Math.imul(h4 ^ (h2 >>> 22), 2869860233);\n        h3 = Math.imul(h1 ^ (h3 >>> 17), 951274213);\n        h4 = Math.imul(h2 ^ (h4 >>> 19), 2716044179);\n        h1 ^= (h2 ^ h3 ^ h4), h2 ^= h1, h3 ^= h1, h4 ^= h1;\n        return [h1 >>> 0, h2 >>> 0, h3 >>> 0, h4 >>> 0];\n    }\n    /**\n     * Clamps the results from the random number generator between the given bounds (inclusive).\n     * @param lower lower bound\n     * @param upper upper bound\n     */\n    clampBetween(lower, upper) {\n        this.lower = lower;\n        this.upper = upper;\n    }\n    /**\n     * Returns the next pseudorandom number in the sequence.\n     * @returns the next number in the sequence\n     */\n    Next() {\n        return this.sfc32() * (this.upper - this.lower + 1) + this.lower;\n    }\n}\n//========================================Cookies================================================================\nfunction setCookie(name, value, expires) {\n    let date = new Date();\n    date.setTime(date.getTime() + expires);\n    document.cookie = `${name}=${value};expires=${date.toUTCString()};path=/`;\n}\n/**\n * Gets the contents of a cookie by a given name. If none found, returns empty string.\n * @param name the name of the cookie to read\n * @returns the contents of a cookie by a given name or null\n */\nfunction getCookie(name) {\n    let fname = name + \"=\";\n    let decodedURI = decodeURIComponent(document.cookie);\n    let cookieList = decodedURI.split(\";\");\n    for (let i = 0; i < cookieList.length; i++) {\n        let cookie = cookieList[i];\n        while (cookie.charAt(0) === \" \") {\n            cookie = cookie.substring(1);\n        }\n        if (cookie.indexOf(fname) === 0) {\n            return cookie.substring(fname.length, cookie.length);\n        }\n    }\n    return null;\n}\n/**\n * Deletes a cookie with the given name\n * @param name name of the cookie to delete\n */\nfunction removeCookie(name) {\n    document.cookie = `${name}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;`;\n}\n//=====================Array Methods===============================\n/**\n * Removes the item at the index from the given array in place.\n * @param array the array to mutate\n * @param index index of the item to be removed from it\n * @returns true if operation was successful, else false\n */\nfunction removeIndex(array, index) {\n    let out = array.splice(index, 1);\n    if (out.length > 0) {\n        return true;\n    }\n    return false;\n}\n/**\n * Removes the item from the given array in place.\n * @param array the array to mutate\n * @param item item to be removed from it\n * @returns true if operation was successful, else false\n */\nfunction removeItem(array, item) {\n    if (array.indexOf(item) > -1) {\n        return removeIndex(array, array.indexOf(item));\n    }\n    return false;\n}\n/**\n * Removes all occurences of the item from the array in place.\n * @param array the array to mutate\n * @param item item to be removed from it in every occurence\n * @returns true if the operation was successful, else false\n */\nfunction removeAll(array, item) {\n    let flag = false;\n    while (array.indexOf(item) >= 0) {\n        if (removeItem(array, item)) {\n            flag = true;\n        }\n    }\n    return flag;\n}\n/**\n * Removes all elements of the given type from the array.\n * @param array the array to mutate\n * @param instance the type of which all instances will be removed from the array\n * @returns true if the operation was successful, else false\n */\nfunction removeInstance(array, instance) {\n    let i = array.length;\n    let flag = false;\n    while (i--) {\n        if (array[i] instanceof instance) {\n            if (removeIndex(array, i)) {\n                flag = true;\n            }\n        }\n    }\n    return flag;\n}\n/**\n * Returns a random permutation of the items in the given array (creates a copy)\n * @param array the array to shuffle\n * @returns the shuffled array\n */\nfunction shuffle(array) {\n    let copy = [...array];\n    let i = copy.length;\n    while (i !== 0) {\n        let j = Math.floor(Math.random() * i);\n        i--;\n        [copy[i], copy[j]] = [copy[j], copy[i]];\n    }\n    return copy;\n}\n//=====================Math Methods===============================\n/**\n * Linearly interpolates between two points\n * @param a point A\n * @param b point B\n * @param t the fraction between the two\n * @returns Vector2 between the two points\n */\nfunction lerp(a, b, t) {\n    if (t > 1 || t < 0) {\n        throw new RangeError(\"Value of t supplied to lerp() must be between 0 and 1 inclusive (got \" + t + \")\");\n    }\n    let i = b.x - a.x;\n    let j = b.y - a.y;\n    i *= t;\n    j *= t;\n    i += a.x;\n    j += a.y;\n    return new Vector2_1.Vector2(i, j);\n}\n//=====================Exports===============================\nexports.Utility = {\n    random: {\n        randInt: randInt,\n        randGaussianInt: randGaussianInt,\n        randItem: randItem,\n        randIndex: randIndex,\n        SeededRandomNG: SeededRandomNG\n    },\n    cookie: {\n        setCookie: setCookie,\n        getCookie: getCookie,\n        removeCookie: removeCookie\n    },\n    array: {\n        removeIndex: removeIndex,\n        removeItem: removeItem,\n        removeAll: removeAll,\n        removeInstance: removeInstance,\n        shuffle: shuffle\n    },\n    math: {\n        lerp: lerp\n    }\n};\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Utility.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/Vector2.ts"
/*!********************************!*\
  !*** ./src/lib/SRL/Vector2.ts ***!
  \********************************/
(__unused_webpack_module, exports) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Vector2 = void 0;\n/**\n * Represents a point in 2d space (x,y)\n */\nclass Vector2 {\n    x;\n    y;\n    /**\n     * @param x pixel on the X axis\n     * @param y pixel on the Y axis\n     */\n    constructor(X, Y) {\n        this.x = X;\n        this.y = Y;\n    }\n    /**\n     * Gets the magnitude of the vector\n     * @returns the magnitude\n     */\n    Magnitude() {\n        return Math.sqrt((this.x * this.x) + (this.y * this.y));\n    }\n}\nexports.Vector2 = Vector2;\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/Vector2.ts?\n}");

/***/ },

/***/ "./src/lib/SRL/index.ts"
/*!******************************!*\
  !*** ./src/lib/SRL/index.ts ***!
  \******************************/
(__unused_webpack_module, exports, __webpack_require__) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.Grid = exports.Sprite = exports.Spritesheet = exports.Bundle = exports.Utility = exports.TextComponent = exports.TextRenderer = exports.SpriteRenderer = exports.SolidRenderer = exports.NullRenderer = exports.AnimationRenderer = exports.Vector2 = exports.Scene = exports.Component = exports.Engine = void 0;\nvar Engine_1 = __webpack_require__(/*! ./Engine */ \"./src/lib/SRL/Engine.ts\");\nObject.defineProperty(exports, \"Engine\", ({ enumerable: true, get: function () { return Engine_1.Engine; } }));\nvar Component_1 = __webpack_require__(/*! ./Component */ \"./src/lib/SRL/Component.ts\");\nObject.defineProperty(exports, \"Component\", ({ enumerable: true, get: function () { return Component_1.Component; } }));\nvar Scene_1 = __webpack_require__(/*! ./Scene */ \"./src/lib/SRL/Scene.ts\");\nObject.defineProperty(exports, \"Scene\", ({ enumerable: true, get: function () { return Scene_1.Scene; } }));\nvar Vector2_1 = __webpack_require__(/*! ./Vector2 */ \"./src/lib/SRL/Vector2.ts\");\nObject.defineProperty(exports, \"Vector2\", ({ enumerable: true, get: function () { return Vector2_1.Vector2; } }));\nvar Renderers_1 = __webpack_require__(/*! ./Renderers */ \"./src/lib/SRL/Renderers.ts\");\nObject.defineProperty(exports, \"AnimationRenderer\", ({ enumerable: true, get: function () { return Renderers_1.AnimationRenderer; } }));\nObject.defineProperty(exports, \"NullRenderer\", ({ enumerable: true, get: function () { return Renderers_1.NullRenderer; } }));\nObject.defineProperty(exports, \"SolidRenderer\", ({ enumerable: true, get: function () { return Renderers_1.SolidRenderer; } }));\nObject.defineProperty(exports, \"SpriteRenderer\", ({ enumerable: true, get: function () { return Renderers_1.SpriteRenderer; } }));\nObject.defineProperty(exports, \"TextRenderer\", ({ enumerable: true, get: function () { return Renderers_1.TextRenderer; } }));\nvar TextComponent_1 = __webpack_require__(/*! ./TextComponent */ \"./src/lib/SRL/TextComponent.ts\");\nObject.defineProperty(exports, \"TextComponent\", ({ enumerable: true, get: function () { return TextComponent_1.TextComponent; } }));\nvar Utility_1 = __webpack_require__(/*! ./Utility */ \"./src/lib/SRL/Utility.ts\");\nObject.defineProperty(exports, \"Utility\", ({ enumerable: true, get: function () { return Utility_1.Utility; } }));\nvar Bundle_1 = __webpack_require__(/*! ./Bundle */ \"./src/lib/SRL/Bundle.ts\");\nObject.defineProperty(exports, \"Bundle\", ({ enumerable: true, get: function () { return Bundle_1.Bundle; } }));\nvar Spritesheet_1 = __webpack_require__(/*! ./Spritesheet */ \"./src/lib/SRL/Spritesheet.ts\");\nObject.defineProperty(exports, \"Spritesheet\", ({ enumerable: true, get: function () { return Spritesheet_1.Spritesheet; } }));\nvar Sprite_1 = __webpack_require__(/*! ./Sprite */ \"./src/lib/SRL/Sprite.ts\");\nObject.defineProperty(exports, \"Sprite\", ({ enumerable: true, get: function () { return Sprite_1.Sprite; } }));\nvar Grid_1 = __webpack_require__(/*! ./Grid */ \"./src/lib/SRL/Grid.ts\");\nObject.defineProperty(exports, \"Grid\", ({ enumerable: true, get: function () { return Grid_1.Grid; } }));\n\n\n//# sourceURL=webpack://kingdom-sim-v2/./src/lib/SRL/index.ts?\n}");

/***/ }

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	const __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		const cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		const module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		if (!(moduleId in __webpack_modules__)) {
/******/ 			delete __webpack_module_cache__[moduleId];
/******/ 			const e = new Error("Cannot find module '" + moduleId + "'");
/******/ 			e.code = 'MODULE_NOT_FOUND';
/******/ 			throw e;
/******/ 		}
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module is referenced by other modules so it can't be inlined
/******/ 	let __webpack_exports__ = __webpack_require__("./src/index.ts");
/******/ 	
/******/ })()
;