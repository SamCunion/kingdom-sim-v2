/**
 * A lord is an autonomous unit that partakes in campaigns and holds a warband
 */
import { Component, Scene } from "./lib/SRL";

export default class Lord extends Component {

    constructor(scene: Scene) {
        super(scene);
    }
}